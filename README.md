# The 210 Project V4.7.1 Hotfix

Changes included:
- Fixed location publish/update button getting stuck on `Publishing...`.
- Button now says `Uploading media...` while photos/videos upload.
- Removed `Insert token` from media rows.
- Kept `Copy token`, `Save caption`, `Set hero`, and `Delete`.
- AI bottom nav now routes to the bottom of the home page so the AI card is visible.
- No new SQL required if V4.6/V4.7 SQL is already applied.
